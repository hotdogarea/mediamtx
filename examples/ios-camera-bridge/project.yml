name: CameraBridgeDemo
options:
  deploymentTarget:
    iOS: "14.0"
targets:
  CameraBridgeDemo:
    type: application
    platform: iOS
    sources:
      - path: DemoHost
        excludes:
          - Info.plist
      - path: CameraBridge
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.CameraBridgeDemo
        INFOPLIST_FILE: DemoHost/Info.plist
        HEADER_SEARCH_PATHS: "$(SRCROOT)/CameraBridge"
        TARGETED_DEVICE_FAMILY: "1"
        CLANG_ENABLE_OBJC_ARC: YES
        CODE_SIGN_STYLE: Automatic
  CameraBridgeCleanHost:
    type: application
    platform: iOS
    sources:
      - path: DemoHost
        excludes:
          - Info.plist
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.example.CameraBridgeCleanHost
        INFOPLIST_FILE: DemoHost/Info.plist
        TARGETED_DEVICE_FAMILY: "1"
        CLANG_ENABLE_OBJC_ARC: YES
        CODE_SIGN_STYLE: Automatic
        GCC_PREPROCESSOR_DEFINITIONS: "$(inherited) CB_CLEAN_HOST=1"
schemes:
  CameraBridgeDemo:
    build:
      targets:
        CameraBridgeDemo: all
  CameraBridgeCleanHost:
    build:
      targets:
        CameraBridgeCleanHost: all
